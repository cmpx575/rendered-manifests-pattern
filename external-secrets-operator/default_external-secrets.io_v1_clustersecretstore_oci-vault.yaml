apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  name: oci-vault
spec:
  provider:
    oracle:
      compartment: staging
      encryptionKey: staging
      principalType: InstancePrincipal
      region: staging
      vault: staging
